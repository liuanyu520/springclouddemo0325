spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/liuanyu520/springclouddemo0325.git
          search-paths:
            - configfiles
      label: dev
server:
  port: 8888